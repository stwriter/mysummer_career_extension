<template>
  <h2>PropVal Demo</h2>
  <!-- Value Only -->
  <p>Value Only:</p>
  <BngPropVal :valueLabel="VALUE" />
  <!-- Key / Value -->
  <p>Key / Value:</p>
  <BngPropVal :keyLabel="KEY" :valueLabel="VALUE" />
  <!-- Icon / Key / Value -->
  <p>Icon / Key / Value:</p>
  <BngPropVal :iconType="icons.beamCurrency" :keyLabel="KEY" :valueLabel="VALUE" />
  <!-- ShrinkNum true -->
  <p>ShrinkNum true:</p>
  <BngPropVal :shrinkNum="true" :valueLabel="-123400000" />
  <BngPropVal :shrinkNum="true" :valueLabel="745600000" />
  <!-- ShrinkNum false -->
  <p>ShrinkNum false:</p>
  <BngPropVal :valueLabel="-123400000" />
  <BngPropVal :valueLabel="745600000" />
  <!-- IconColor -->
  <p>IconColor:</p>
  <BngPropVal :iconType="icons.beamCurrency" :keyLabel="KEY" :valueLabel="VALUE" :iconColor="'var(--bng-orange-b400)'" />
  <!-- Valueless -->
  <p>Empty value:</p>
  <BngPropVal :iconType="icons.beamCurrency" keyLabel="Valueless" valueLabel="" />
  <p>No gap:</p>
  <BngPropVal :noGap="true" :iconType="icons.beamCurrency" :keyLabel="KEY" :valueLabel="VALUE" />
</template>

<script setup>
import { BngPropVal, icons } from "@/common/components/base"

const KEY = "key is here"
const VALUE = "value is here"
</script>

<style scoped>
p {
  color: #666;
}
</style>

<script>

// Demo Metadata
// -------------------------------------------------------
import source from "./bngPropVal_demo.vue?raw"
export default {
  source,
  title: "Display a property with an associated value",
  description: `A standardised way to display a property (key) with an associated value. Optionally with an icon`,
  propInfo: [
    {
      name: "iconType",
      type: "String/Object",
      desc: "Optional icon to display. See `BngIcon` for further explanation of this property value",
    },
    {
      name: "keyLabel",
      type: "String",
      desc: "Text for the property name/key",
    },
    {
      name: "valueLabel",
      type: "Any",
      desc: "Value to display for the property",
    },
    {
      name: "shrinkNum",
      type: "Boolean",
      desc: "Switch on/off 'shrinking' of numeric values (see demo)",
    },
    {
      name: "iconColor",
      type: "String",
      desc: "CSS colour for the icon. Default is `'#fff'`",
    },
    {
      name: "noGap",
      type: "Boolean",
      desc: "Switch to turn on/off the gaps between icons and labels, and between labels",
    },
  ],
  attrInfo: [

  ],
}

</script>

